#ifndef ERRORCODE_H
#define ERRORCODE_H

enum ErrorCode{
    // Common
    NO_ERR = 0,
    INVALID_ARGS = 1,
    INVALID_CALC_OPT = 2,

    // Reference
    INVALID_REF_ELECTION = 100
};

#endif // ERRORCODE_H
